In your case this file should have information of user remote_user public key which was generated before running the docker compose build for the image remote-host.
More information check the Dockerfile under centos7 folder.

Commands i used to generate the user ssh keys as ->  ssh-keygen -f remote-key